import { CheckInputDirective } from './check-input.directive';

describe('CheckInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckInputDirective();
    expect(directive).toBeTruthy();
  });
});
